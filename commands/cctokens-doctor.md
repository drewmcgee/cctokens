# cctokens-doctor

Run:

```bash
cctokens doctor --last
```

Summarise only:
- Logged token totals (input, output, cache read, cache write).
- Top 3 context-waste findings with evidence.
- Exact recommended next action for each finding.

Do not paste long JSON output. Do not include raw transcript content.
